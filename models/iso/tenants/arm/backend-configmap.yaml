apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-config
  # namespace applied at apply time
data:
  tenants.json: |
    {
      "arm": { "tenantId": "arm", "schemaName": "arm", "displayName": "ARM", "redisDb": 1, "isActive": true }
    }
