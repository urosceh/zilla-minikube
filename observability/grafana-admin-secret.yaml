apiVersion: v1
kind: Secret
metadata:
  name: zilla-grafana-admin
  namespace: monitoring
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: zilla-observability
type: Opaque
stringData:
  # Local Minikube credentials only. Never replace these with real credentials.
  admin-user: admin
  admin-password: zilla-local-only
